{
  "name": "identity-starter",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "dev:web": "pnpm --filter @identity-starter/web dev",
    "dev:admin": "pnpm --filter @identity-starter/admin dev",
    "dev:all": "turbo run dev",
    "db:generate": "pnpm --filter @identity-starter/db generate",
    "db:migrate": "pnpm --filter @identity-starter/db migrate",
    "db:migrate:status": "pnpm --filter @identity-starter/db migrate:status",
    "db:migrate:reset": "pnpm --filter @identity-starter/db migrate:reset",
    "db:push": "pnpm --filter @identity-starter/db push",
    "db:studio": "pnpm --filter @identity-starter/db studio",
    "db:check": "pnpm --filter @identity-starter/db check",
    "db:seed": "pnpm --filter @identity-starter/db seed",
    "test:e2e": "./scripts/e2e.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "lefthook": "^2.1.4",
    "turbo": "^2.8.20",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.32.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "lefthook",
      "@node-rs/argon2"
    ]
  },
  "engines": {
    "node": ">=24"
  }
}
